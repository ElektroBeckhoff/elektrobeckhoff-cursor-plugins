---
description: Safety rules for FBD/FUP-to-ST migration. Enforces preview-first workflow, backup requirements, and TODO reporting.
---

# TwinCAT FBD/FUP Migrator Safety

## Rule 1: Never skip the preview step

Before writing any files, always run with `dry_run=true` first and verify:
- Error count is 0
- TODO count is acceptable
- Generated ST preview looks correct

Exception: If the user explicitly says "just run it" or "skip preview", proceed with the default swap mode (which creates a backup).

## Rule 2: Never use replace without backup

The combination `replace=true` + `backup=false` permanently destroys the original FBD file with no recovery option. This combination MUST NOT be used unless:
- The user explicitly requests it AND
- The user confirms they have external version control (git, SVN, etc.)

If the user asks for `replace=true`, always keep `backup=true` (which is the default).

## Rule 3: Prefer no-swap for first migration

When migrating a file or project for the first time, prefer `swap=false` so the user can review the generated files without affecting the TwinCAT project. Only use swap mode (default) when the user confirms the migration quality is acceptable.

## Rule 4: Always use recursive for folders

When the input is a folder, always add `recursive=true` unless the user explicitly says "top-level only" or "no subfolders".

## Rule 5: Report TODOs clearly

If the migration produces TODO markers, always:
1. Report the exact count
2. List the first 5 TODO reasons
3. Explain that these networks need manual review
4. Suggest searching for `TODO [FBD Migration]` in the generated code

## Rule 6: Do not migrate non-NWL files

The tool automatically skips files that are already ST, CFC, SFC, or IL. Do not attempt to force migration of these files. Report them as skipped.

## Rule 7: Verify after migration

After any non-dry-run migration, recommend:
1. Open the TwinCAT project in XAE (`twincat_open`)
2. Run `twincat_check_all_objects`
3. Review any compiler errors
4. Search for TODO markers

## Rule 8: Respect strict for safety-critical projects

If the user mentions safety-critical, SIL, or PL requirements, always add `strict=true` to abort on any unclear logic. Do not proceed with partial migration in these contexts.

## Rule 9: File types

- `.TcPOU` -- primary migration target (PROGRAM, FUNCTION_BLOCK, FUNCTION, METHOD, ACTION)
- `.TcGVL` -- loaded but skipped (Global Variable Lists have no implementation)
- `.TcDUT` -- loaded but skipped (Data Unit Types have no implementation)
- Other extensions -- rejected at file collection stage

## Rule 10: Encoding

Default encoding is UTF-8. The tool auto-detects UTF-8-BOM and Latin-1 as fallbacks. Do not change `encoding` unless the user reports encoding errors.
